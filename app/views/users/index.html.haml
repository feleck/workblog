%h3 Users
%ul.users
  - @users.each do |user|
    %li
      = link_to user.name, user
      signed up #{user.created_at.to_date}
      = ', has posts: ' + user.posts.count.to_s + ', comments: ' + user.comments.count.to_s + '.'